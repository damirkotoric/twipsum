{
  "name": "twipsum",
  "description": "twitter to lorem ipsum generator",
  "main": "app.js",
  "version": "1.0.0",
  "engines": {
    "node": "6.10.1"
  },
  "scripts": {
    "start": "node app.js",
    "sass": "node-sass -w --output-style compressed -o public/css/ src/sass/",
    "uglify": "mkdir -p public/js && uglifyjs src/js/*.js -m -c -o public/js/app.js"
  },
  "private": true,
  "dependencies": {
    "nconf": "^0.7.1",
    "twit": "^2.2.5"
  },
  "author": "@damirkotoric",
  "devDependencies": {
    "node-sass": "^4.5.3",
    "uglify-js": "^3.0.10"
  }
}
